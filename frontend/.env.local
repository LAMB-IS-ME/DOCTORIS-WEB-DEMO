# Local environment variables (do NOT commit this file)
## Gemini API key (used when USE_N8N_RAG is not true)
GEMINI_API_KEY=sk-replace-with-your-real-key

## n8n RAG integration (optional)
# NOTE: Vite only exposes variables prefixed with VITE_ to the client.
# Keep legacy keys for readability, but set VITE_ duplicates used by code.

# Frontend toggles
USE_N8N_RAG=true
VITE_USE_N8N_RAG=true

# Public webhook URL exposed by n8n (e.g. https://your-n8n-host/webhook/abc123)
N8N_WEBHOOK_URL=http://localhost:5678/webhook/doctoris-chat
VITE_N8N_WEBHOOK_URL=http://localhost:5678/webhook/doctoris-chat

# Optional API key/token if your webhook requires it (sent as Authorization: Bearer <token>)
N8N_API_KEY=
VITE_N8N_API_KEY=
